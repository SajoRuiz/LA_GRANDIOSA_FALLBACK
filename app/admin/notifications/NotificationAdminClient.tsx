"use client";
import { useState } from "react";
import styles from "./notifications.module.css";
export default function NotificationAdminClient(){const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");const[error,setError]=useState("");async function process(){setBusy(true);setMessage("");setError("");try{const response=await fetch("/api/internal/notifications/process",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({limit:25})});const result=await response.json() as any;if(!response.ok)throw new Error(result.error??"Notification processing failed.");setMessage(`Claimed ${result.claimed} notification(s).`);window.location.reload();}catch(e){setError(e instanceof Error?e.message:"Notification processing failed.");}finally{setBusy(false)}}return <div className={styles.controls}>{error?<p className={styles.error}>{error}</p>:null}{message?<p className={styles.success}>{message}</p>:null}<button disabled={busy} onClick={process}>{busy?"Processing…":"Send queued notifications now"}</button></div>}
